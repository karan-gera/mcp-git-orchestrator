{
  "name": "@mcp-git-orchestrator/cli",
  "version": "0.1.0",
  "description": "CLI for MCP Git Orchestrator server",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "mcp-git": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "clean": "rm -rf dist",
    "lint": "echo \"No linting configured yet\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@mcp-git-orchestrator/server": "workspace:*",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "files": [
    "dist"
  ]
}
